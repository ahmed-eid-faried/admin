import 'package:adminapp/core/class/appsizestd.dart';

// Appsize size = Get.find(); //for all app , don't refresh in web
// Appsize size = Appsize(); //for all this screen
class Appsize extends AppsizeStd {
  // note
  // double get hp => height / 100;
  // double get wp => width / 100;
  // double get name => value * wp;
  /////////////////////////////////////////////////////////////////////////////
}
